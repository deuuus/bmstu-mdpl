PUBLIC PRINT_NUM

EXTRN NUMBER : WORD

DSEG SEGMENT PARA PUBLIC 'DATA'
	BUFFER DB 17 DUP (?)
DSEG ENDS

CSEG SEGMENT PARA PUBLIC 'CODE'
	ASSUME DS:DSEG, CS:CSEG

PRINT_NUM PROC NEAR
	
	MOV AH, 09H
	MOV DX, BX
	INT 21H
	
PRINT_NUM ENDP

CSEG ENDS
END